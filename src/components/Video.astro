---
const { id, title = "Video" } = Astro.props;
---
<iframe
  width="100%"
  style="aspect-ratio: 1/1; vertical-align: top; margin-bottom: 0.2rem; border-radius: 8px;"
  src={`https://www.youtube.com/embed/${id}`}
  title={title}
  frameborder="0"
  allowfullscreen
/>
